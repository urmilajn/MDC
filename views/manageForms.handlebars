{{#if errors}}
    {{#each errors}}
        <div class="alert alert-danger">{{msg}}</div>
    {{/each}}
{{/if}}

<div class="header clearfix">
<nav>
  <ul class="nav nav-pills pull-right">
    <form method="get" action="/customer/manageUsers">
      <button type="submit" class="btn btn-default" formaction="/customer/manageForms">Manage Forms</button>
      <button type="submit" class="btn btn-default" formaction="/customer/manageUsers">Manage Users</button>
    </form>
  </ul>
</nav>
<h4>{{customerName}}</h4>
</div>


<form method="get" action="/customer/addForm">
  <button type="submit" class="btn btn-default">Add Forms</button>
</form>
<p></p>



<form method="get" action="/form">
  <div class="btn-group btn-group-justified">
    <a href="/customer/getForms" class="btn btn-default">View All Forms</a>
   </div>
</form>


<p></p>
{{#if forms}}
  <div>
      <table class="table table-bordered">
        <thead>
            <th>FormName</th> 
            <th colspan="2">Fields</th> 
            <th/>
        </thead> 
        <tbody> 
            {{#each forms}} 
            <tr>
              <td>{{formName}}</td>
              <td>
                  Field Name: 
                  {{#each fields}}  
                    <ul>
                     <li>{{name}}</li>
                    </ul>
                  {{/each}} 
              </td>
              <td>
                  Field Type: 
                  {{#each fields}} 
                  <ul>
                    <li>{{type}}</li>
                  </ul>
                {{/each}} 
              </td>
                <td width="100">
                      <button type="submit" class="glyphicon glyphicon-minus btn btn-link" formaction="/customer/removeForm"/>
                    </form>
                </td>
            </tr>
            {{/each}} 
        </tbody> 
    </table>
</div>
{{/if}}